# Rename this file to .env and fill in your values before running the server.

# Secret key used to sign JWT tokens.
# Generate a secure value with: python -c "import secrets; print(secrets.token_hex(32))"
BACKEND_SECRET_KEY=change-me-in-production-use-a-long-random-string

# JWT access-token lifetime (minutes). Default is 1440 (24 hours).
# BACKEND_ACCESS_TOKEN_EXPIRE_MINUTES=1440
